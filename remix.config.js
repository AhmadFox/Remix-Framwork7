module.exports = {
	serverBuildTarget: "netlify",  // This configures Remix to output server-side code for Netlify
	serverBuildPath: "build/server/index.js",  // Path where the server build is generated
	// Optional: Add any other custom configurations here
};
